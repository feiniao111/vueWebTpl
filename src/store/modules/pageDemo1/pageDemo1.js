/**
 * demo vuex文件
 */
import * as types from '../../mutation-types'
import Account from '../../../api/pageDemo1/pageDemo1'
const state = {
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
    state,
    getters,
    actions,
    mutations
}